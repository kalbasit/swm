language: go
go:
  - 1.8
cache:
  directories:
    - vendor
    - $HOME/.glide
before_install:
  # Install Glide
  - go get -u github.com/Masterminds/glide
before_script:
  - glide install
  - go get -u golang.org/x/tools/cmd/cover
script: go test $(glide nv)
